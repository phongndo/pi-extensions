amends "package://github.com/jdx/hk/releases/download/v1.51.0/hk@1.51.0#/Config.pkl"
import "package://github.com/jdx/hk/releases/download/v1.51.0/hk@1.51.0#/Builtins.pkl"

// Use workspace-local tools via Bun without downloading missing commands.
local bun = new Step {
  prefix = "bun run --bun"
}

local linters = new Mapping<String, Step> {
  ["oxfmt"] = (Builtins.oxfmt) {
    ...bun
    batch = true
  }
  ["ox_lint"] = (Builtins.ox_lint) {
    ...bun
    batch = true
  }
  ["tsc"] = (Builtins.tsc) {
    ...bun
    // Typecheck is whole-project and read-only.
    stomp = true
  }
}

hooks {
  ["pre-commit"] {
    fix = true
    stash = "git"
    steps = linters
  }
  ["pre-push"] {
    steps = linters
  }
  ["check"] {
    steps = linters
  }
  ["fix"] {
    fix = true
    steps = linters
  }
}
